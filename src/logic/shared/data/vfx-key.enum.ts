export enum VFXType {
  Particles = 'particles',
  Decals = 'decals',
}

export enum ParticleKey {
  SmallExplosion = 'small',
  MediumExplosion = 'medium',
  BigExplosion = 'big',
}

export enum DecalKey {
  GroundCracks = 'ground_craks',
}
